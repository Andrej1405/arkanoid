name: Run lint
on: push

jobs: 
  run_format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm i
      - name: Validate linter rules
        run: npm run lint

